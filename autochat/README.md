# Autochat Service for LunaBot

### Usage

1. Install dependencies (Python 3.8+ required):

```bash
uv sync
```

2. Update the configuration of LunaBot by modifying `config/chat/autochat.yaml`.

3. Run the service:

```bash
python serve.py
```

