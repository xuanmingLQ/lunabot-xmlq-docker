# Sekai Event Tracker Service for LunaBot

### Usage

1. Install dependencies (Python 3.8+ required):

```bash
pip install -r requirements.txt
```

2. Run the service:

```bash
python src.services.event_tracker.main
```

3. Configurations can be found in `config/sekai/sekai.yaml` (shared with LunaBot).
